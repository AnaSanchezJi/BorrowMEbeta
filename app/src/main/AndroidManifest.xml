<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ana.borrowmebeta">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/brmicon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".act_recuperar"
            android:screenOrientation="portrait"/>
        <activity android:name=".act_logrecuperados"
            android:screenOrientation="portrait"
            />
        <activity android:name=".act_logprestamos"
            android:screenOrientation="portrait"
            />
        <activity android:name=".act_prestamo"
            android:screenOrientation="portrait"
            />
        <activity android:name=".act_prestamoapi21"
            android:screenOrientation="portrait"
            />
    </application>

</manifest>